version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: secad-api:latest
    container_name: secad-api
    ports:
      - "8088:8088"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - TZ=America/Bogota
      - ConnectionStrings__DbOracle=${DB_ORACLE}
      - ConnectionStrings__DbCoest=${DB_COEST}
      - AppSettings__UploadsPath=/app/uploads
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8088/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  uploads:
